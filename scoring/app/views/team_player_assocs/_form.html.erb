<%= form_for(@team_player_assoc) do |f| %>
  <% if @team_player_assoc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team_player_assoc.errors.count, "error") %> prohibited this team_player_assoc from being saved:</h2>

      <ul>
      <% @team_player_assoc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :team_id %><br />
    <%= f.number_field :team_id %>
  </div>
  <div class="field">
    <%= f.label :player_id %><br />
    <%= f.number_field :player_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
